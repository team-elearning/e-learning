module.exports = {
  projects: [
    {
      root: './frontend',
      tsconfig: './frontend/tsconfig.app.json',
      // Nếu dùng TypeScript, thêm tsconfig vào đây
      // Nếu dùng JS, chỉ cần root
    },
  ],
}
